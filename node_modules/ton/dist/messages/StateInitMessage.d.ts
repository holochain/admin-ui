import { Cell } from "../boc/Cell";
import { Maybe } from "../types";
import { Message } from "./Message";
export declare class StateInitMessage implements Message {
    readonly code: Cell | null;
    readonly data: Cell | null;
    constructor(opts: {
        code?: Maybe<Cell>;
        data?: Maybe<Cell>;
    });
    writeTo(cell: Cell): void;
}
